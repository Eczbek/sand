```bash
git clone https://github.com/Eczbek/sand
cd sand
cmake -S. -Bbuild
cmake --build build
./build/sand
```

- `W`/`A`/`S`/`D` to move
- ENTER or SPACE to place or select tile
- `E` to open inventory
- `Q` to unselect tile
- `\` or `R` to replace tile
- `~` to save and quit
